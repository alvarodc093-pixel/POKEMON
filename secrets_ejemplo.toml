# Copia este fichero a .streamlit/secrets.toml (crea la carpeta .streamlit si no existe)
# y pega tu API key de https://ollama.com/settings/keys
#
# IMPORTANTE: secrets.toml NUNCA se sube a GitHub (por eso va en el .gitignore).
# Este fichero de ejemplo si se puede subir: no contiene la key de verdad.
#
# Si no pones key, la app funciona igualmente con el modelo local (qwen3.5:4b).

OLLAMA_API_KEY = ""
